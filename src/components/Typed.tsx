import { ReactTyped } from "react-typed";

const Typed = () => (
    <div>
        <ReactTyped strings={["I'm a FullStack Web Developer", "I'm a lover of Christ"]} typeSpeed={40} backSpeed={50} loop />
        <br />
    </div>
);

export default Typed;
